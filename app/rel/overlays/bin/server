#!/bin/sh
set -eu

export POD_A_RECORD=$(echo $POD_IP | sed 's/./-/g')
export RELEASE_DISTRIBUTION=name
export RELEASE_NODE=<%= @release.name %>@${POD_A_RECORD}.${NAMESPACE}.pod

cd -P -- "$(dirname -- "$0")"
PHX_SERVER=true exec ./app start
